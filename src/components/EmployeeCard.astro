---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';


// Employees https://raw.githubusercontent.com/heyheynic/frontend-design-skoleopgave/main/resources/json/employees.json


// const { img, name, title, description, social_links} = Astro.props;
const { employee } = Astro.props;
const { img, name, title, description, social_links} = employee;

const images = import.meta.glob<{ default: ImageMetadata }>('/src/assets/images/*.{jpeg,jpg,png,gif}')


---

<article>

</article>



<style>
article {
    background-color: blue;
}
</style>